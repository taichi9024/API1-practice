<%= form_with(model: fuge) do |form| %>
  <% if fuge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fuge.errors.count, "error") %> prohibited this fuge from being saved:</h2>

      <ul>
        <% fuge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
